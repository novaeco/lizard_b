#include "esp_log.h"
#include "esp_http_server.h"

static const char* TAG = "HTTP_HANDLERS";

// Placeholder pour les handlers HTTP
void http_handlers_init(void)
{
    ESP_LOGI(TAG, "Handlers HTTP initialisés");
}