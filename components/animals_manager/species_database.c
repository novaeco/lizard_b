#include "animals_manager.h"
#include "esp_log.h"

static const char* TAG = "SPECIES_DATABASE";

// Placeholder pour la base de données des espèces
void species_database_init(void)
{
    ESP_LOGI(TAG, "Base de données des espèces initialisée");
}