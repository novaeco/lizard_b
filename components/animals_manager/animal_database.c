#include "animals_manager.h"
#include "esp_log.h"

static const char* TAG = "ANIMAL_DATABASE";

// Placeholder pour la base de données des animaux
void animal_database_init(void)
{
    ESP_LOGI(TAG, "Base de données des animaux initialisée");
}