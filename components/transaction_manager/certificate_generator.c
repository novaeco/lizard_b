#include "transaction_manager.h"
#include "esp_log.h"

static const char* TAG = "CERTIFICATE_GENERATOR";

void certificate_generator_init(void)
{
    ESP_LOGI(TAG, "Générateur de certificats initialisé");
}