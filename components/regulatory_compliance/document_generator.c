#include "regulatory_compliance.h"
#include "esp_log.h"

static const char* TAG = "DOCUMENT_GENERATOR";

void document_generator_init(void)
{
    ESP_LOGI(TAG, "Générateur de documents initialisé");
}